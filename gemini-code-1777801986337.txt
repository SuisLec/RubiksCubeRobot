你好！我目前正在开发一个双臂解魔方机器人项目。硬件接线已经全部完成，现在我需要你帮我写一段“最基础的电机转动测试代码”，用来验证我的硬件连接是否正常。

请基于以下我提供的硬件清单、引脚映射和配置信息，为我生成代码。

【一、 核心硬件型号】
1. 主控板：野火 STM32F103VET6 拂晓开发板 (PlatformIO board: genericSTM32F103VE)。
2. 电机驱动器：2 个 EBF-MSD4805 步进电机驱动器。
3. 步进电机：2 个 57 步进电机（型号：J-5718HBS2401，步距角 1.8°）。
4. 供电：独立 6S 航模电池（22.2V）为驱动器供电。

【二、 关键接线方式（非常重要）】
我的开发板与驱动器之间采用的是**“共阳极接法”**：
- 驱动器端的 PUL+、DIR+、ENA+ 已经全部物理短接，并统一连接到了开发板的 VCOM（+5V）引脚上。
- 因此，开发板的 GPIO 引脚连接的是驱动器的负极端子（PUL-、DIR-、ENA-）。
- 逻辑说明：GPIO 输出 LOW（0）时，驱动器内部光耦导通（信号有效）；GPIO 输出 HIGH（1）时，光耦截止（信号无效）。对于 ENA（使能）引脚，通常保持 HIGH（或不操作）即为默认使能锁定状态，输出 LOW 会让电机脱机释放。

【三、 真实硬件引脚映射】
请严格使用以下引脚配置 GPIO：
- 电机 1（左臂）：
  - 脉冲引脚 (PUL-)：PC6
  - 方向引脚 (DIR-)：PE6
  - 使能引脚 (ENA-)：PE5
- 电机 2（右臂）：
  - 脉冲引脚 (PUL-)：PC7
  - 方向引脚 (DIR-)：PD15
  - 使能引脚 (ENA-)：PC13

【四、 驱动器拨码参数设置】
- 细分设置：16 细分。
- 脉冲计算公式：因为是 1.8° 步进电机 + 16 细分，所以让电机完整旋转一圈（360°）需要 3200 个脉冲。

【五、 你的任务要求】
请使用 STM32 HAL 库（C语言）为我编写一段在 main.c 中运行的测试代码。要求如下：
1. 提供完整的 GPIO 初始化代码（将上述 6 个引脚配置为推挽输出模式，初始电平设为高电平 HIGH）。
2. 写一个简单的发脉冲函数 `StepMotor(int motor_id, int steps, int dir)`。其中脉冲的翻转使用简单的 `HAL_GPIO_WritePin` 配合 `HAL_Delay()` 或微秒级延时即可（这是验证测试，暂时不需要用定时器中断）。
3. 在 `while(1)` 主循环中，实现这样的动作逻辑：
   - 让电机 1 正转一圈（3200个脉冲），停顿 1 秒；
   - 让电机 2 反转一圈（3200个脉冲），停顿 1 秒；
   - 循环往复。
4. 代码请尽量带上中文注释，方便我阅读。